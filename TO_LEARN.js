// [DONE] => learn about namespaces and cgroups in docker and  PID1;
// [DONE] => learn about docker-networking;
// [DONE] => learn about docker-volumes;
// [] => learn about ssl and how it work and what are ssl certificates; 
// => {
    // driver_opts:
    // type: none
    // o: bind
    // device: /home/mamazzal/data/mariadb
// }


// How Docker and docker compose work
// The difference between a Docker image used with docker compose and without docker compose
// The benefit of Docker compared to VMs

// ssl => && openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/nginx/ssl/nginx-selfsigned.key -out /etc/nginx/ssl/nginx-selfsigned.crt -subj "/C=MA/ST=Martil/L=Martil/O=Example/CN=mamazzal.42.fr"
//mariadb => --botstrap
// docker daemon => docker daemon